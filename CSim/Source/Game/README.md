# Game

Cellular-automata **game domain**: canvas state, game module (modes), and editor-facing tools (brush/cursor).

This is not a generic “stdlib.” Prefer putting engine host code in `Engine/` and shared utilities in `Services/` or `Foundation/`.
