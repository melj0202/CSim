\section{Project overview}
\label{sec:overview}

\subsection{What CSim is}
CSim is an interactive \textbf{cellular automata} (``cell'') simulator with a
2D canvas, multiple rule sets (Game of Life, Brian's Brain, Wireworld, \ldots),
camera navigation, edit/run modes, save/load hooks, and a small in-app command /
debug surface. Rendering is currently OpenGL via GLFW/GLEW (with platform
entry stubs for Linux/macOS).

\begin{inferencebox}
Historically this grew from a simpler GoL program into a mini engine shell
(``Illumo''): modules, env vars, input contexts, backend interfaces, Tracy hooks.
The product goal remains a cell simulator; the engine shape is partly for skill
practice and multi-API future-proofing.
\end{inferencebox}

\subsection{High-level system diagram}

\begin{center}
\begin{tikzpicture}[
  font=\small,
  box/.style={draw, rounded corners, align=center, inner sep=6pt, minimum height=1.1cm},
  arr/.style={-{Latex}, thick}
]
  \node[box, fill=blue!8] (plat) {Platform\\entry + SaveLoad};
  \node[box, fill=blue!8, right=1.1cm of plat] (app) {App\\CellMain};
  \node[box, fill=green!10, right=1.1cm of app] (eng) {Engine\\Illumo + modules};
  \node[box, fill=orange!10, below=1.0cm of eng] (game) {Game + Rulesets};
  \node[box, fill=purple!8, right=1.1cm of eng] (rnd) {Rendering\\tokens / Backend};
  \node[box, fill=gray!10, below=1.0cm of plat] (svc) {Services + Foundation};

  \draw[arr] (plat) -- (app);
  \draw[arr] (app) -- (eng);
  \draw[arr] (eng) -- (rnd);
  \draw[arr] (eng) -- (game);
  \draw[arr] (svc) -- (eng);
  \draw[arr] (svc) -- (app);
\end{tikzpicture}
\end{center}

\subsection{Runtime one-liner}
Platform \texttt{main} $\rightarrow$ \symbolref{CellMain} $\rightarrow$ construct
\symbolref{Illumo} $\rightarrow$ \symbolref{Init} (services + modules) $\rightarrow$
loop \{\,\symbolref{Update}, \symbolref{Render}\,\} until window close.

\subsection{Non-goals (current)}
\begin{openbox}
Confirm or rewrite. Draft non-goals:
\begin{itemize}
  \item Not a general-purpose AAA renderer.
  \item Not multiplayer / networking.
  \item Not a full ECS game engine (entity scaffolding exists but is thin).
  \item Perfect Linux/macOS parity is secondary to Windows correctness for now.
\end{itemize}
\end{openbox}
