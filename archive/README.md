# Archive

Not part of the CSim build.

- `old-states/` — previous State-pattern experiments (class-per-mode FSM) kept for reference while the game uses an enum-based mode switch in `Game/CellGameModule`.
