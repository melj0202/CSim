// ChainedPoolAlloc is a header-only class template (see PoolAlloc.h).
// This translation unit is intentionally empty so older build lists that still
// name PoolAlloc.cpp remain harmless if reintroduced.
